library(dplyr)
library(stringr)

options(stringsAsFactors = FALSE)

# Load custom utility functions if they exist
if (file.exists("src/utils/helpers.R")) {
  source("src/utils/helpers.R")
}

# Set default options for the project
options(report_template_path = "templates/report_template.qmd")
options(infosheet_template_path = "templates/physician_info_template.qmd")
options(default_output_dir = "reports")